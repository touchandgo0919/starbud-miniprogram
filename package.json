{
  "name": "starbud-miniprogram",
  "version": "1.1.5",
  "private": true,
  "scripts": {
    "check": "node scripts/validate.mjs",
    "test": "npm run check && node --test --test-concurrency=1 tests/*.test.cjs"
  }
}
